app:
    resource: '@AppBundle/Controller/'
    type: annotation

user:
    resource: "@UserBundle/Controller/"
    type: annotation

customer:
    resource: "@CustomerBundle/Controller/"
    type: annotation
